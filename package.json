{
  "name": "openpartsflow",
  "private": true,
  "description": "Root scripts delegate to frontend/; run backend separately (uvicorn).",
  "scripts": {
    "dev": "npm run dev --prefix frontend",
    "dev:3002": "npm run dev:3002 --prefix frontend",
    "build": "npm run build --prefix frontend",
    "lint": "npm run lint --prefix frontend"
  }
}
